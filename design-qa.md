# WhoAmI UI 验证记录

日期：2026-09-07

## 验证范围

原生 SwiftUI iPhone UI：今天、记录编辑器、日记、复盘、七维档案和项目/问题管理。无账号、邀请码、付费墙、强制建档或服务配置前置步骤。此记录为开发验证结果，不设置运行或仓库审批门禁。

## 视觉参考与实际界面

- 视觉参考：`design/reference/today-concept.png`，853 × 1844 px，设计目标 390 × 844pt。
- 主实现截图：`design/screenshots/today.png`、`journal.png`、`review.png`、`profile.png`，iPhone 16e，1170 × 2532 px，390 × 844pt，3× 密度。
- 额外设备：iPhone 17 / iPhone 17 Pro，1206 × 2622 px，402 × 874pt。
- 全图对照：`design/qa/reference-390.png` 与 `design/qa/today-390.png`，分别归一到 390 × 843 px 与 390 × 844 px，在同一轮输入中打开并比较。
- 细节证据：原始分辨率的 `design/screenshots/record.png`、`original-evidence.png`、`local-review.png`、`feedback-saved.png`。
- 状态：浅色、中文界面、初始示例；编辑器包含测试文字。真实记录测试使用隔离的测试内容。

设计参考用于确定风格与层级。原生实现保留系统安全区、导航栏、键盘和 iOS Tab 栏；参考不含系统栏，因此不宣称逐像素一致。顶部系统空间使首屏展示的列表行少于概念图，后续内容可正常滚动，主要操作保持可见。角色使用同方向新生成的独立素材，未将整张设计稿当作界面。

## 五项视觉检查

| 检查面 | 结果 |
| --- | --- |
| 字体与层级 | 中文标题、正文和辅助信息层级清楚；原生系统字体；关键按钮与输入区可辨认。已查看原始分辨率正文与反馈编辑区。 |
| 间距与布局 | 390pt 与 402pt 宽度下无横向溢出。原生 Tab 栏可达，编辑器软键盘出现后保存、分类和心情入口仍可使用。长列表允许滚动。 |
| 色彩 | 暖白基底、珊瑚色主要动作、鼠尾草辅助信息保持一致。主按钮采用较参考更深的珊瑚色以改善白字可读性。 |
| 图像 | 陪伴角色清楚、无拉伸；首页和档案中有合适裁切。图标 1024 × 1024、不带 alpha，无设备外框素材冒充原生界面。 |
| 文案 | 示例持续标记，真实回顾明确为本地整理，AI 尚未接入。未记录的心情使用询问文案，不冒充用户状态。 |

## 已发现并处理的问题

1. **[P2] 复盘主动作被长示例卡片推到首屏之外。** 将“回看这几天”移到周期选择下方。最终 `review.png` 显示主按钮无需滚动即可找到，小屏重新执行复盘入口测试通过。
2. **[P2] 两处复盘周期选项不一致。** 设置和复盘首页统一为 2/3/4 天，共用持久化状态。
3. **[P2] 编辑示例项目或问题会丢失示例标记。** 编辑后保留标记，清除示例不会把虚构材料留作真实内容。
4. **[P2] 清除示例项目可能留下关联。** 清理示例时同步解除真实记录和草稿对这些项目的引用，保留记录正文。
5. **[P2] 记录后补心情的文案缺少操作入口。** 记录编辑器增加心情及项目关联选项，与初次录入保持一致。

首轮截图查看了首页、日记、档案和键盘状态。上述修改后重新构建，并在 iPhone 16e 再次验证四页导航、复盘入口、原文与反馈流程；最终截图来自修订后的应用。没有待处理的 P0/P1/P2 视觉或核心流程问题。

## 构建与交互验证

- Xcode 26.3 Debug Simulator 构建成功；最终 UI 修改后再次构建成功。
- iPhone 17（运行结果报告 iOS 26.3.1）：4 个测试，0 失败，0 跳过。
- iPhone 16e（同运行时）：最终小屏复查 2 个测试，0 失败，0 跳过。
- 两轮覆盖 **5 个不同测试用例，合计 6 次成功执行**。第二轮重新执行四页导航，并新增本地回顾与反馈验证。

| 用例 | 结果 |
| --- | --- |
| 无登录直接记录、今天/日记展示、重启仍保留 | 通过 |
| 草稿收起恢复、保存后清空草稿 | 通过 |
| 四个 Tab、复盘示例、跳回原文依据 | 两种设备通过 |
| 编辑取消、保存修改、确认删除 | 通过 |
| 本地复盘只用真实记录、排除示例、保存并重读行动状态和反馈 | 通过 |

结果摘要：`design/test-results-summary.json`、`design/test-results-final-summary.json`。完整 `.xcresult` 位于本地 `build/`，未提交庞大的运行产物。主要检查截图保存在 `design/screenshots/`。

构建日志仅有未使用 AppIntents 的元数据提示和测试框架无需剥离签名的工具提示，无 Swift 编译错误。版本化文件通过 `git diff --check`。

## 当前边界

- 仅验证模拟器，未在物理 iPhone 上测试或发布 App Store/TestFlight。
- AI、独立录音、云同步、定时通知及备份导入尚未实现；当前数据整理在本地完成。
- 七维、项目与设置已实现界面和本地操作；未对每个表单组合进行穷举测试。
- 深色、横屏、iPad、超大辅助功能字体和完整 VoiceOver 路径留待后续适配；本轮未宣称覆盖。

final result: passed
