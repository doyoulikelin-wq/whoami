> 最新 v0.3.2 独立记录与当日火焰验证见 [本轮验证](design/独立记录验证.md)。

> 最新 v0.3.1 时间、导入导出与真机数据保留验证见 [本轮验证](design/导入导出验证.md)。下文为 v0.3 轮盘交互的历史验证。

# WhoAmI v0.3 · 七维轮盘 Demo 验证

日期：2026-09-07

## 验证对象与结论

原生 SwiftUI / iOS 17+。底部三个 Tab 为总览、记录、对比。记录页使用七等分轮盘、七个自行绘制的矢量图标、正文与语音、三档黑色火焰心境条及保存按钮。七个维度独立保留草稿，保存形成只读快照。

本轮 iPhone 竖屏 Demo 验证通过，未发现遗留的 P0/P1/P2 阻断问题。语音真实转写和未覆盖设备范围单列在文末。

视觉依据为用户明确的七维轮盘、三板块、三 Tab 规格，以及 [UI 设计说明](design/UI设计说明.md) 中的冷峻设计系统。没有新增登录、邀请码、付费或仓库审核流程。

## 图像与状态

- 主设备：iPhone 16e，390 × 844pt，1170 × 2532px，3× 密度，iOS 26.3.1。
- 辅助设备：iPhone 17 Pro，402 × 874pt，用于旧版 JSON 兼容验证。
- 当前三个页面：[总览](design/screenshots/dashboard.png)、[记录](design/screenshots/record-wheel.png)、[对比](design/screenshots/comparison.png)。[组合预览](design/screenshots/overview.png) 只排列原始截图，不重绘界面。
- 细节：[键盘输入](design/screenshots/record-keyboard.png)、[只读详情](design/screenshots/comparison-detail.png)、[不变的历史快照](design/screenshots/immutable-snapshots.png)。
- 完整功能复测证据：[七维黑焰](design/screenshots/wheel-saved.png)、[七条存档](design/screenshots/seven-archives.png)、[松手后只剩火苗](design/screenshots/mood-flame.png)。这些截图来自完整复测，早于最终正文区缩短20pt和深色条对比度调整；控制行为未改变。
- 截图来自不同的隔离测试状态，英文正文均为测试内容。旧版暖色概念图和 v0.2 雕塑首页截图仅作为历史资料。

## 视觉核对

实际打开并检查全屏图；将首轮与最终记录页均缩放为390 × 844进行同尺寸比较：[首轮](design/qa/record-wheel-first-390.png)、[最终](design/qa/record-wheel-final-390.png)。

| 检查面 | 观察结果 |
| --- | --- |
| 布局与操作层级 | 三个原生 Tab；首页以状态列表和活动统计为主。记录页顺序为轮盘、正文/心境、保存，没有额外编辑面板。 |
| 轮盘与图标 | 轮盘约占三分之一屏幕，七区等分；只显示图标，标题在正文上方。已存档火焰位于外缘，七个图标均未被遮挡。 |
| 字体与引导 | 维度名称25pt，正文16pt，引导短句12pt；标题与占位提示区分清晰，默认状态不出现数值。 |
| 色彩与火焰 | 冷白、石墨黑、钢灰一致；能量条三段渐深、边界明确。低/中/高火焰轮廓和体量有差别。最深背景69747E与白字的对比度约4.77:1。 |
| 编辑与历史 | 键盘上方“完成”可见且可点。初始保存按钮已露出；选定心境后条形控件收起，保存按钮与底栏有间隔。详情只显示正文、日期与心境，无编辑入口。 |

## 检查与修正

1. **[P2，已修复] 首次输入时 keyboard toolbar 的“完成”偶尔不出现。** 首轮测试复现后，改为跟随输入焦点、固定在键盘上方的44pt收起栏。完整复测与最终相关复测均通过，并核对了最终键盘截图。
2. **[P2，已修复] 初始记录页垂直密度过高，保存按钮位于底栏后方。** 缩小板块间隔，正文常态100pt、编辑156pt；保留大轮盘。最终截图中常态保存按钮可见，选完心境后更充裕。
3. **轮盘斜滑误切维度。** 增加起始方向锁定；纵向滚动交给外层页面，明确横向或沿弧滑动才切维度。
4. **心境不能由默认值代填。** 保存要求已有正文和实际选定心境，未选时同一保存按钮显示“选择心境后保存”。没有新增确认面板；记录页不显示或朗读数值。
5. **输入与语音目标隔离。** 正文绑定捕获创建时的维度，避免迟到的输入法提交写入新维度；语音启动、录音及收尾期间暂停正文编辑。保存等待最终转写或一秒超时，过期回调不能改写旧会话。

首轮还有一处测试读取错误：AX child 的 value 为空字符串，遮掉了可见数字 label。修正读取后验证低/中/高实际值为16、50、84；没有为通过测试改变产品数值逻辑。

## 数据兼容

在专用 iPhone 17 Pro 模拟器上直接升级未带新版字段的旧文件，逐项比较旧5条记录与草稿，内容完全相同；新增字典正常初始化。旧记录缺少心境时保持缺省，不推测补造。

修改后的保存创建带日期快照，原历史不回写。无变化的重复保存复用快照。未保存的草稿不会进入历史详情。七维草稿切换、重启恢复和逐一保存均由 UI 测试验证。

## 构建与测试

Xcode 26.3 Simulator Debug 构建成功；无 Swift 编译错误。仅有未使用 AppIntents.framework 时的元数据提取提示。

- 首轮4项：2项通过；键盘收起与测试数值读取问题修复后复测。
- 完整复测 **4/4通过，0失败**：[机器结果](design/test-results-wheel-summary.json)。覆盖三 Tab/轮盘手势、七维草稿重启恢复与逐一保存、三档拖动及归档数值、修改快照/重复保存/未保存草稿隔离/只读详情。
- 最后两处布局与对比度调整后，相关的键盘/快照和三 Tab/轮盘测试再次 **2/2通过，0失败**：[机器结果](design/test-results-wheel-polish-summary.json)。这两项是重复验证，不是新增用例。
- 本地原始结果：build/WhoAmI-wheel-final-UI.xcresult、build/WhoAmI-wheel-polish-UI.xcresult。构建与测试临时文件不提交仓库。
- 最后进行了控件与数据绑定的独立只读检查，未发现新的提交阻断问题。

## 已知边界

- 语音权限文案已进入编译后的 Info.plist；Speech/AVAudioEngine 的启动、完成与清理通过编译。未使用环境麦克风进行真实中文转写，真机识别效果尚未实测。
- 超大辅助功能字号、完整 VoiceOver 路径、横屏、iPad 未覆盖；火焰尊重系统减弱动态效果设置。
- AI 分析、云同步、定时提醒未接入。支持设备端语音识别时在设备端处理，否则可能使用 Apple 系统服务。

final result: passed
