# v0.3.1 时间与文件功能验证

日期：2026-09-07

## 已完成

- 总览最新记录显示日期与时分；对比列表、详情显示时分秒。保存仍使用原始时间戳，导出带毫秒及 UTC 偏移。
- 总览底部“导入与导出”提供今天、指定日期、全部范围及 Markdown/JSON 文件格式。
- 系统分享面板实际显示“保存到文件”；JSON 导入实际打开系统文件选择器。文件取消后原记录不变。
- 导入以 UUID 合并，已有内容优先；写盘成功后才更新内存。清除示例仅匹配 isDemo=true，不修改真实记录及草稿。

## 自动验证

完整测试 **24/24 通过**：18 项单元测试与 6 项 UI 测试。[机器结果](test-results-transfer-summary.json)

包含原文/时间往返、空心境旧字段、日期与夏令时、无示例导出、错误格式/时间/数值/重复 ID、导入合并、写盘失败、损坏原库保护，以及原有轮盘/火焰/七维草稿流程。新增 UI 测试验证总览和对比的时间、三个导出范围、JSON 系统分享、导入文件选择器与取消保留。

补充午夜亚毫秒边界测试曾发现日期标签被 DateFormatter 舍入到次日；已改用 Calendar 年月日分量，时间戳向下保留毫秒，严格范围校验保持。修复后文件格式测试 **13/13 通过**。[补充结果](test-results-transfer-boundary-summary.json)。其中12项为复测、1项为新增，因此共有25个不同用例通过。

Release 真机签名构建通过。系统分享产生的实际 JSON 文件还经过独立读取核对：格式 whoami.records、版本1、Asia/Shanghai 时区、1条测试记录，毫秒时间存在，正文与UI输入相同。

## 截图核对

- [含记录时间的总览](screenshots/dashboard.png)
- [含时分秒的对比](screenshots/comparison.png)
- [导入与导出](screenshots/data-transfer.png)
- [系统文件分享](screenshots/system-share.png)
- [JSON 文件选择器](screenshots/json-file-picker.png)

在 iPhone 16e 390×844pt 截图中，时间未挤压维度名称；文件页单屏可见范围、格式、导出和导入入口，主次动作明确。系统分享呈现有效文件附件和本地保存动作。所有截图仅使用隔离测试内容，没有使用个人真机记录。

## 真机数据保留

更新前已将 iPhone Air 的本地库备份在被 Git 忽略的 build/device-backups 目录。个人记录原文未写入日志、截图或仓库。

v0.3.1 已安装并在 iPhone Air 启动。取得升级前后本地库后，逐字段比较整份 JSON：仅删除明确标记的示例对象，所有真实记录的正文、UUID、时间、心境以及七维/旧版草稿均完全相同。核对结果通过；原始报告只保存在本机 build/iphone-air-preservation-check.json，未提交个人库。

## 范围

只导出已保存记录；草稿、项目详情和复盘不在本格式中。Markdown用于阅读/AI分析，重新导入使用本App JSON，单文件上限20MB。实际向微信联系人发送文件未执行，分享选项取决于手机已安装的扩展。语音转写未在本轮重新测试。

final result: passed
